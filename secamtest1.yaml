esphome:
  name: s3camtest1
  friendly_name: S3CamTest1

esp32:
  board: seeed_xiao_esp32s3
  variant: esp32s3
  framework:
    type: arduino
    version: latest
    platform_version: 6.4.0

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "OxZhO8p8e2hyLhdiBZUP8pns1a/LnWxzpq5x0tBONKw="

ota:
  password: "19b38fb7fb62e3b086bca4dbcadbc6a2"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "S3Camtest1 Fallback Hotspot"
    password: "SPER22bkaRal"

psram:
  mode: octal
  speed: 80MHz



esp32_camera:
  name: My XAIO Camera 1
  external_clock:
    pin: GPIO10
    frequency: 20MHz
  i2c_pins:
    sda: GPIO40
    scl: GPIO39
  data_pins: [GPIO15, GPIO17, GPIO18, GPIO16, GPIO14, GPIO12, GPIO11, GPIO48]
  vsync_pin: GPIO38
  href_pin: GPIO47
  pixel_clock_pin: GPIO13
  # reset_pin: GPIO48
  resolution: 640x480
  jpeg_quality: 10
  
web_server:
  port: 80

esp32_camera_web_server:
  - port: 8080
    mode: stream
  - port: 8081
    mode: snapshot  

sensor:
  
  - platform: wifi_signal
    name: 
      "S3 Cam Test 1 WiFi Signal Sensor"
    update_interval: 60s

captive_portal:
    
